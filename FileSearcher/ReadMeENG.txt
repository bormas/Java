FileSearcher searches files and directories in project folder and ouputs their attributes.
For extended attributes you need to input "--a" before all list of names.
Regular expressions are supported.
For obtaininng information about all files input only "*"
Example: 
1) out Java.*
2) --a prod.* 
3) FileSearcher.class src